# JobView Android（Kotlin + Compose）

该目录存放 Kotlin Compose 客户端源码，采用多模块架构（App / Core / Feature）。  
构建与运行：

```bash
cd mobile/JobViewAndroid
./gradlew tasks       # 首次运行会下载 Gradle
./gradlew assembleDebug
```

> 需要本地已配置 Android SDK（API 34+）与 JDK 17。
